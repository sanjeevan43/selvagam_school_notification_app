import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Selvagam Santhanalakshmi Noble School | Futuristic Educational Excellence",
  description: "A world-class futuristic educational experience in Tirukazhukundram, Tamil Nadu. Building bright futures with knowledge, discipline, and excellence.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
